name: scio
channels:
  - conda-forge
  - defaults
dependencies:
  - python>=3.10
  - jupyterlab
  - numpy
  - pandas
  - scipy
  - matplotlib
  - pyserial
  - cryptography
  - pytest
  - pip
# The `tp` conda env on this machine already satisfies these and is what the
# notebooks were developed against.
